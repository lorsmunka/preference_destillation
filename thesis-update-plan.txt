THESIS.TXT UPDATE PLAN
======================

Updated: 2026-04-04
Purpose: Track what's changed since thesis.txt was written, so it can be
         updated section by section without losing anything.


=== CURRENT PROJECT STATE ===

10 completed runs, 1 in progress, 3 more queued.
Two domains fully implemented: reddit_comment_sentiment, math_word_problem.
Reduced input vocabulary implemented and tested across both domains.
Temperature annealing, KL annealing, mini-eval all implemented.

Completed runs:
  SCALE experiments (full input vocab, 7 runs):
    tsc-scale-20h-2L-5M        (20h, 2L, 1 head, ~5M params)
    tsc-scale-40h-2L-10M       (40h, 2L, 1 head, ~10M params)
    tsc-scale-64h-2L-17M       (64h, 2L, 1 head, ~17M params)
    tsc-scale-128h-4L-36M      (128h, 4L, 2 heads, ~36M params)
    tsc-scale-192h-6L-53M      (192h, 6L, 3 heads, ~53M params)
    tsc-scale-256h-8L-74M      (256h, 8L, 4 heads, ~74M params)
    tsc-scale-384h-12L-127M    (384h, 12L, 6 heads, ~127M params)

  REDUCED INPUT VOCAB experiments (3 runs):
    tsc-reduced-input-proj-5M-64h-8L    (64h, 8L, 1 head, ~5M params)
    tsc-reduced-input-proj-10M-128h-5L  (128h, 5L, 2 heads, ~10M params)
    tsc-reduced-input-proj-36M-320h-9L  (320h, 9L, 5 heads, ~36M params)

  In progress:
    tsc-temp-anneal-mild-80h-5L-6M  (80h, 5L, 1 head, ~6M, temp 3->1)

  Queued:
    tsc-temp-anneal-80h-5L-6M       (80h, 5L, 1 head, ~6M, temp 5->1)
    tsc-kl-anneal-80h-5L-6M         (80h, 5L, 1 head, ~6M, KL 0.99->0.4)
    tsc-kl-anneal-wide-80h-5L-6M    (80h, 5L, 1 head, ~6M, KL 0.9->0.1)


=== SCOPE EXPANSION (thesis describes ~10% of current project) ===

The thesis was written around a single-domain, single-model pilot run.
The project is now a multi-domain, multi-config experimental pipeline
with completed scaling experiments and active ablation studies.

Key additions not in thesis at all:
  - Math word problem domain (14 operation types, 40+ templates per op, ~31 token vocab)
  - Queue-based pipeline (data gen queue + training queue)
  - Scaling experiments: 7 completed runs (5M to 127M params) — RESULTS AVAILABLE
  - Reduced input vocabulary: 3 completed runs — RESULTS AVAILABLE
    (68k tokens for reddit = 73.9% reduction, 1.3k for math = 99.5% reduction)
  - Temperature annealing experiments: 1 running, 1 queued (3->1 and 5->1)
  - KL ratio annealing experiments: 2 queued (0.99->0.4 and 0.9->0.1)
  - Mini-eval system (mid-epoch evaluation every 1000 batches)
  - Inference speed benchmarking (implemented in analysis/inference.py)
  - Confusion matrices per category (implemented)
  - Cloud GPU cost analysis and scaling estimates
  - All annealing uses cosine decay schedule (not linear)


=== SECTION-BY-SECTION DRIFT ===

ABSTRACT
  Status: UPDATED (2026-04-08)
  - Now mentions both domains (sentiment + math)
  - Mentions scaling experiments, temp annealing, KL-CE annealing
  - Cites concrete results (87%+ classification, 93%+ student only, 98%+ teacher forced)
  - Remaining: could be tightened further once all results sections exist

1. BEVEZETES
  Status: UPDATED (2026-04-08)
  - 1.1 now mentions both domains (reddit sentiment + math word problem)
  - "kutatási pipeline" framing, minimális módosítással új domain hozzáadható
  - E/1 → általános alany
  - Remaining: cost analysis section could reference GPU guidance work

2. IRODALOMKUTATAS
  Status: UPDATED (2026-04-08)
  - 2.4: KL/CE arány és temp annealing now described as implemented, not just cited
  - 2.5: input vocab reduction added (was "input vocabulary teljes marad")
  - 2.8: 537M → 5M-127M model range
  - 2.12: "teljes input vocabulary" → ki- és bemeneti redukció
  - E/1 → általános alany
  - Remaining: no new sources added (DTKD etc. not used/read)

3. IMPLEMENTACIO
  3.1 Pelda
    Status: NEEDS MATH EXAMPLE (not yet done)
    - Only shows Reddit example — add math example too

  3.2 Data extraction pipeline
    Status: UPDATED (2026-04-08)
    - Now describes both domains (reddit + math with 13 operation types, scaffold structure)
    - Vocab sizes corrected: ~525 reddit, ~528 math (both with auxiliary)
    - Reduced input vocab mentioned
    - E/1 → általános alany
    - Remaining: queue system details not described (implementation detail)

  3.3 Tanitas
    Status: UPDATED (2026-04-08)
    - Forward KL divergence explained with MiniLLM [6] context
    - Temperature annealing added (cosine decay, configurable start→end)
    - LR warmup + cosine decay added
    - Mini-eval added (configurable frequency and batch count)
    - KL/CE ratio now described as configurable, not fixed 90-10
    - "kap" → "kaphat" (nem minden kísérlet használja)
    - Test ratio corrected (10% → configurable, default 2%)
    - E/1 → általános alany
    - Remaining: queue system, run directory structure, structured logging not described

  3.4 Student modell
    Status: UPDATED (2026-04-08)
    - 3.4: pilot framing kept, "később" → "a projekt bővült 5M-127M-ig"
    - 3.4.1: paraméterezhető architektúra, formula, pilot vs reduced vocab kontraszt
      * Gemma 3 4b embedding mérete (262,208 × 2560 = ~671M) hozzáadva
      * Vocab csökkentés = lineáris embedding paramétercsökkentés kifejtve
      * 384h/12L példa: 230M → 55M (76% csökkenés), body 12% → 52%
    - 3.4.2: input+output vocab mindkét domainre, context lengths, 34x-860x méretcsökkentés
    - 3.4.3: modellméret range-ek, 7 scale + 3 reduced input kísérlet
    - E/1 → általános alany

4. MODSZERTAN
  4.1 Pilot run
    Status: KEEP AS HISTORY, ADD NEW RESULTS
    - Pilot results are still valid history, keep as-is
    - But must add new sections for actual experiment results:
      * Scaling curve results (7 sizes, accuracy vs params)
      * Reduced input vocab results (3 runs, compare vs full vocab at same param budget)
      * Temperature annealing results (when complete)
      * KL annealing comparison results (when complete)

  4.2 Korabbi pilot
    Status: KEEP AS-IS
    - Fine, good to keep the encoding bug story

  4.3 Pilot teszt elemzes
    Status: KEEP AS HISTORY
    - Fine as historical context

  4.4 Kiertkelesi peldak
    Status: EXPAND
    - Add math examples alongside sentiment
    - Reference confusion matrices
    - Reference inference benchmarks
    - Show scaling comparison (small model vs large model outputs)

  4.5 Erőforrás becslés (renamed from "Korai becslés")
    Status: UPDATED (2026-04-08)
    - Data gen: ~3 mp/példa, A100 20GB szelet, ~417 óra / 500k példa
    - Training: 0.5 mp/batch, RTX 4070, ~6.5 óra / modell / 3 epoch
    - Forward pass optimization explained (40-50x → 1 pass per example)
    - Bottleneck clearly identified: data gen, not training

5. FORRASOK
  Status: NEEDS ADDITIONS
  - 25 sources, solid foundation
  - Add: DTKD (Wei et al., 2024) for dynamic temperature KD
  - Add: Adaptive Temperature Distillation (2025) if cited
  - May need math word problem generation references
  - Consider: DTS (Dynamic Temperature Scheduler, 2025)


=== NEW SECTIONS NEEDED ===

These don't exist in thesis.txt but should be added:

1. EXPERIMENTAL DESIGN
   - Scaling study: 7 model sizes (5M to 127M), controlled hyperparams, COMPLETED
   - Reduced input vocab: 3 runs at matched param budgets, COMPLETED
   - Temperature annealing: 2 configs (3->1, 5->1), IN PROGRESS/QUEUED
   - KL ratio annealing: 2 configs (0.99->0.4, 0.9->0.1), QUEUED
   - All experiments use same domain (reddit_comment_sentiment) for fair comparison
   - Architecture: same formula, balanced depth/width scaling
   - Controls: identical hyperparams except the variable being tested

2. RESULTS
   - Scaling curve: accuracy vs parameter count from 7 completed runs
   - Reduced input vocab: compare 5M/10M/36M reduced vs full vocab equivalents
   - Temperature annealing: compare 3->1 vs 5->1 vs KL-only baseline
   - KL annealing: compare 0.99->0.4 vs 0.9->0.1 schedules
   - Cross-run metrics: final eval accuracy, convergence speed, loss curves
   - Key questions to answer:
     * Where is the knee in the scaling curve?
     * Does reduced input vocab help at same param budget?
     * Is temp annealing better than KL annealing?
     * How aggressive should KL annealing be?

3. MATH DOMAIN DETAILS
   - Problem generation (14 operation types, 40+ templates each)
   - Semantic categories: arithmetic, compound, comparison
   - Word problem variables (names, items, quantities)
   - Scaffold structure: "Problem: ...\nA={val}\nB={val}\nC=A+B={result}\nSolution: {result};"
   - 31-token output vocabulary (digits, operators, variable names, keywords)
   - 1,282-token reduced input vocabulary (99.5% reduction)
   - How it differs from sentiment:
     * Longer sequences (210 vs 75 max)
     * Numeric output vs categorical
     * Different evaluation (exact match vs category match)
     * Much smaller vocab (31 vs 525 output, 1.3k vs 68k input)

4. INFRASTRUCTURE
   - Queue system (data gen queue + training queue, JSON-based)
   - Run organization: runs/{name}/ with checkpoints/, logs/, info.json
   - Structured logging: training.jsonl, mini_eval.jsonl, eval_epoch.jsonl
   - Resumability: checkpoint save/load, graceful shutdown (% key)
   - Mini-eval: periodic evaluation during training (every 1000 batches)
   - Cloud deployment considerations


=== WHAT TO KEEP AS-IS ===

- Literature review (section 2) — solid, just minor additions
- Pilot run history (4.1, 4.2, 4.3) — good narrative, shows iteration
- Architecture component descriptions (RMSNorm, RoPE, GeGLU) — still accurate
- The encoding bug story — examiners love honest debugging narratives


=== RECOMMENDED REWRITE ORDER ===

DONE (2026-04-08):
1. Section 3.4 (Student model) ✓
2. Section 4.5 (Cost estimates → Erőforrás becslés) ✓
3. Section 3.2 (Data pipeline) ✓
4. Section 3.3 (Training) ✓
8. Abstract ✓
9. Section 2 (Literature) ✓
+  Section 1.1 (Bevezetés scope) ✓
+  E/1 → általános alany (frissített szekciókban) ✓

TODO (needs run results from other machine):
5. Add new "Experimental Design" section — backbone of the thesis
6. Add new "Results" section — populate with data from 10 completed runs
7. Add new "Math Domain" section — fully implemented, worth documenting

TODO (minor):
- 3.1 Példa: add math example alongside reddit
